# Memory Analysis Engine — Completion Matrix

| Sub-Analyzer / Requirement | Covered Volatility 3 Plugins & Artifact Types | Status | Primary Sub-Analyzer Class & Rules |
|---|---|---|---|
| **1. Process Analyzer** | `windows.pslist`, `windows.psscan`, `windows.pstree`, `windows.cmdline`, `memory.pslist`, `memory.psscan`, `memory.pstree`, `memory.cmdline`, `process_record`, `unlinked_process_record` | COMPLETE | `ProcessAnalyzer` (`memory.process_analyzer`): Detects `psscan != pslist` unlinked processes (`T1057`), orphan processes, and parent-child hierarchy anomalies (e.g. `lsass.exe` parent != `wininit.exe`). |
| **2. DLL Analyzer** | `windows.dlllist`, `windows.ldrmodules`, `windows.modules`, `memory.dlllist`, `memory.ldrmodules`, `memory.modules`, `dll_record`, `dll_load` | COMPLETE | `DLLAnalyzer` (`memory.dll_analyzer`): Detects PEB loader list inconsistencies (`InLoad`/`InInit`/`InMem`) and loaded modules from user-writable directories (`C:\Users\`, `C:\Temp\`, `T1574.001`). |
| **3. Memory Network Analyzer** | `windows.netscan`, `windows.netstat`, `memory.netscan`, `memory.netstat`, `network_connection` (`volatility3`) | COMPLETE | `MemoryNetworkAnalyzer` (`memory.network_analyzer`): Extracts memory-resident sockets, protocol states, remote endpoints, and owning PIDs (`T1049`). Preserves independent provenance from PCAP telemetry. |
| **4. Injection Analyzer** | `windows.malfind`, `windows.vadinfo`, `windows.vaddump`, `memory.malfind`, `memory.vadinfo`, `memory.vaddump`, `injection_indicator` | COMPLETE | `InjectionAnalyzer` (`memory.injection_analyzer`): Detects executable private memory regions (`PAGE_EXECUTE_READWRITE` / `RWX`) and malfind injected code tags (`T1055`, `T1620`). |
| **5. Rootkit Analyzer** | `memory.rootkit`, `unlinked_process_record`, `hidden_modules` | COMPLETE | `RootkitAnalyzer` (`memory.rootkit_analyzer`): Detects kernel structure anomalies and hidden processes/modules (`T1014`). |
| **6. Credential Analyzer** | `memory.lsass`, `memory.credentials`, `memory.hashdump`, `memory.lsadump` | COMPLETE | `CredentialAnalyzer` (`memory.credential_analyzer`): Detects credential structure presence in memory (`T1003`). **Redacts all raw passwords/hashes**; persists zero secrets to Findings or FIR. |
| **7. Timeline Analyzer** | Timestamped memory artifacts (process creation/exit, net connection, module load) | COMPLETE | `TimelineAnalyzer` (`memory.timeline_analyzer`): Constructs UTC memory event temporal sequences (process start -> network connection). Skips missing timestamps. |
| **8. Memory Engine Orchestrator** | Orchestrates all 7 sub-analyzers | COMPLETE | `MemoryAnalysisEngine` (`memory_engine.py`): Deduplicates findings on `(case_id, source_artifact_id, layer, fact)` and merges `contributing_correlation_ids`. |
| **9. Router Integration** | Map all 25 memory artifact types to `"memory"` | COMPLETE | `argus/forensic_analysis/router.py` |
| **10. Orchestrator Integration** | Register `"memory": MemoryAnalysisEngine()` in `ENGINE_REGISTRY` | COMPLETE | `argus/forensic_analysis/orchestrator.py` |
| **11. Unit Test Suite** | 19 test functions verifying dispatch, sub-analyzers, redaction, security AST, and orchestrator handoff | COMPLETE | `argus/tests/unit/test_memory_analysis.py` (19/19 PASSED, 61/61 total suite PASSED) |
