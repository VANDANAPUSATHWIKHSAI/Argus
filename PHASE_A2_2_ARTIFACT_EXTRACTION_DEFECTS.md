# ARGUS — Phase A.2.2 Artifact Extraction Defects & Gap Audit Report

**Date**: August 31, 2026  
**Audited Engine**: `preprocessing/artifact_extractor/extractor.py`  
**FCR Target**: `preprocessing/fcr_engine/`

---

## 1. Deduplication Boundary Audit

- **Requirement**: Artifact extraction deduplication MUST NOT be confused with FCR or Layer-3 finding deduplication.
- **Identity Key Formula**:
  $$\text{DeduplicationKey} = \text{hash}(\text{case\_id} \| \text{evidence\_id} \| \text{source\_artifact\_id} \| \text{artifact\_type} \| \text{normalized\_value})$$
- **Verification**: Distinct records with similar human-readable strings maintain separate identity keys if originating from different source artifacts or byte offsets.

---

## 2. Format Gaps & AFF Status

- **AFF Container Status**: Both AFF files (`ntfs1-gen0.aff`, `ntfs1-gen1.aff`) remain explicitly **`BLOCKED_MISSING_LIBAFF`**.
- **No Attempted Enablement**: Per contract constraints, no AFF libraries were installed or modified.
- **Integrity**: Original SHA-256 hashes preserved 100%.

---

## 3. Downstream FCR Contract Compatibility Audit

- **FCR Schema Requirements**:
  - `CorrelationRecord` requires `correlation_id` matching `^CORR-[0-9]{5,}$`.
  - `artifact_ids` requires $\ge 2$ unique artifact IDs.
  - `relationship_type` requires supported relationship types (`temporal_proximity`, `shared_ioc`, `process_tree`, `network_process`).
- **Audit Result**: Extracted artifacts generated by `ArtifactExtractor` pass all FCR schema validation rules without requiring any modifications to `preprocessing/fcr_engine/schemas.py` or `engine.py`.
